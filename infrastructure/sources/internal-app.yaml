apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: internal-app
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/SoundaryaA3098/helm-chart
  secretRef:
    name: https-credentials
  ref:
    branch: master